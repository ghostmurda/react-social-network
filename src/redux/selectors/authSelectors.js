export const getUserId = (state) => state.authData.userId;

export const getIsFetching = (state) => state.authData.isFetching;

export const getUserName = (state) => state.authData.userName;

export const getAuth = (state) => state.authData.auth;